## user.py: 
Operation related to the users, such as login, registry (will initialize user data files), edit config, add watchlist.
- Please refer to user_data_example.yaml

## server.py:
Service keeps running, refresh info to cache (item logs), send notification.

### **Both user.py & server.py are running on server.**